#include <iostream>
#include <vector>
#include <string>   
#include <algorithm>
#include "Chessboard.hpp"
#include "GenMoves.hpp"
#include <sstream>

bool Mate(bool turn) {
    if (turn) {
        
        }

    else if (!turn) {

    }
    else {
        std::cout << "Idk who's turn it is but you guys gotta figure something out man" << std::endl;
    }
}